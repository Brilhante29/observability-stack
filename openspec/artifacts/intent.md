# Intent: #25 observability-stack

Prove a real controlled HTTP incident whose identity remains connected across
metrics, traces and logs. The machine gate is three measured runs with
`signal_correlation_rate = 1.0`; production SLO and cloud parity are non-goals.
